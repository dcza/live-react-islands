{
  "name": "live-react-islands",
  "version": "0.1.0",
  "private": true,
  "description": "React Islands for Phoenix LiveView - embed React components in LiveView with bidirectional communication and SSR support",
  "repository": {
    "type": "git",
    "url": "https://github.com/dcza/live-react-islands.git"
  },
  "author": "David Czaplinski",
  "license": "MIT",
  "workspaces": [
    "packages/elixir/*",
    "packages/js/*",
    "examples/*"
  ],
  "scripts": {
    "build": "yarn build:js && yarn build:elixir",
    "build:js": "yarn workspaces foreach -A --topo --exclude live-react-islands run build",
    "build:elixir": "cd packages/elixir/live_react_islands && mix compile",
    "test": "yarn test:js && yarn test:elixir",
    "test:js": "yarn workspaces foreach -A --exclude live-react-islands run test",
    "test:elixir": "cd packages/elixir/live_react_islands && mix test",
    "clean": "yarn clean:js && yarn clean:elixir",
    "clean:js": "yarn workspaces foreach -A --exclude live-react-islands run clean",
    "clean:elixir": "cd packages/elixir/live_react_islands && mix clean",
    "format": "yarn format:js && yarn format:elixir",
    "format:js": "yarn workspaces foreach -A --exclude live-react-islands run format",
    "format:elixir": "cd packages/elixir/live_react_islands && mix format"
  },
  "engines": {
    "node": ">=18.0.0",
    "yarn": ">=3.0.0"
  },
  "packageManager": "yarn@4.12.0"
}
